---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "{{.Name}} {{.Type}} - {{.RenderedProviderName}}"
subcategory: ""
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{.Name}} ({{.Type}})

{{ .Description | trimspace }}

## Example Usage

### Minimal

A single VM at a site from your project allocation.

{{ tffile "examples/resources/fabric_slice/basic.tf" }}

### With a GPU component

{{ tffile "examples/resources/fabric_slice/with_gpu.tf" }}

### With an NVMe component

{{ tffile "examples/resources/fabric_slice/with_nvme.tf" }}

### With a SharedNIC component

{{ tffile "examples/resources/fabric_slice/with_shared_nic.tf" }}

### With a SmartNIC and a VLAN sub-interface

{{ tffile "examples/resources/fabric_slice/with_smart_nic.tf" }}

### With an L2Bridge network (same site)

{{ tffile "examples/resources/fabric_slice/with_l2bridge.tf" }}

### With a FABNetv4 routed network

{{ tffile "examples/resources/fabric_slice/with_fabnetv4.tf" }}

### With a FABNetv4Ext network

{{ tffile "examples/resources/fabric_slice/with_fabnetv4ext.tf" }}

### With an L3VPN network

{{ tffile "examples/resources/fabric_slice/with_l3vpn.tf" }}

### With an L2STS network (cross-site)

{{ tffile "examples/resources/fabric_slice/with_l2sts.tf" }}

### With an L2PTP network (cross-site)

{{ tffile "examples/resources/fabric_slice/with_l2ptp.tf" }}

### With a facility port

{{ tffile "examples/resources/fabric_slice/with_facility_port.tf" }}

### Multi-site

{{ tffile "examples/resources/fabric_slice/multisite.tf" }}

{{ .SchemaMarkdown | trimspace }}

## Import

Import is supported using the following syntax:

{{ codefile "shell" "examples/resources/fabric_slice/import.sh" }}

Import recovers the slice topology and computed runtime fields from the
orchestrator. SSH key material is write-only and is not stored in FABRIC, so
`ssh_keys` (or the deprecated `ssh_key`) and `ssh_key_version` are not recovered
on import; set them in configuration to match the keys the slice was created
with, or the next plan shows a replacement.
